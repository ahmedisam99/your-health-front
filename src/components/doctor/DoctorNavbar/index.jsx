import { Layout } from 'antd';

import styles from './style.module.css';

const { Header } = Layout;

export default function DoctorNavbar() {
  return <Header className={styles.navbar}>ناف بار</Header>;
}
